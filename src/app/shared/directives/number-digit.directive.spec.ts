import { NumberDigitDirective } from './number-digit.directive';

describe('NumberDigitDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberDigitDirective();
    expect(directive).toBeTruthy();
  });
});
